#ifndef CRYPTION_HPP
#define CRYPTION_HPP

#include <string>

int executeCryption(const std::string& taskData);

#endif


// #ifndef CRYPTION_HPP
// #define CRYPTION_HPP

// #include <string>
// #include <vector>

// class Cryption {
// public:
//     static std::vector<unsigned char> encrypt(const std::vector<unsigned char>& plaintext, const std::string& key);
//     static std::vector<unsigned char> decrypt(const std::vector<unsigned char>& ciphertext, const std::string& key);
//     static int executeCryption(const std::string& taskData);
// };

// #endif